# 患者自动接待系统 — 业务需求与实施方案

## 一、业务流程总览

```
员工加患者好友 → 员工在自建应用生成专属客服链接 → 发给患者
    → 患者扫码进入客服会话
    → 系统自动发欢迎语+询问手机尾号
    → 患者回复尾号 → 查患者库 → 返回群链接
    → 纸质方案：请求上传照片 → 多模态LLM识别图片 → 写入客户描述
    → 电子方案：方案内容直接写入客户描述
    → 打标签(月份+医院) + 改备注(医院全称+建档姓名)
    → 未回复尾号：24h后再问 → 48h仍未回复：发名单给员工+备注加"-未回复"
```

---

## 二、需求拆解与实施状态

### 需求1：患者信息数据库表 ✅ 已完成

**要求**：建 `patients` 表，字段如下：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| patient_name | TEXT | 患者姓名 |
| phone_tail | TEXT | 手机尾号(4位) |
| hospital | TEXT | 医院全称 |
| plan_type | TEXT | 方案类型：`electronic`（电子）/ `paper`（纸质） |
| plan_content | TEXT | 方案内容（电子方案填写，纸质方案留空） |
| group_link | TEXT | 群链接 |
| created_at | TEXT | 创建时间 |

**测试数据**：

| patient_name | phone_tail | hospital | plan_type | plan_content | group_link |
|---|---|---|---|---|---|
| 张三 | 8860 | 北京协和医院 | electronic | 低盐低脂饮食方案：每日钠摄入<2g... | https://work.weixin.qq.com/group1 |
| 徐子忱 | 5786 | 上海瑞金医院 | paper | (空) | https://work.weixin.qq.com/group2 |

---

### 需求2：患者进入客服自动问候 ✅ 已完成

- 患者扫码/点链接 → 企微推送 `enter_session` 事件 → 自动发欢迎语+询问手机尾号
- `enter_session` 同时从 scene 提取 `contact_external_userid`（wob ID）存入 `reception_progress`

---

### 需求3：尾号查询 → 群链接 → 方案处理 ✅ 已完成

**流程**：

```
患者回复尾号 (如 "8860")
    ↓
查询 patients 表: SELECT * FROM patients WHERE phone_tail='8860' ORDER BY created_at DESC LIMIT 1
    ↓
┌─ 找到患者 ──────────────────────────────────────┐
│  回复: "已为您匹配！您的专属交流群：{group_link}" │
│                                                    │
│  if plan_type == 'paper':                          │
│    追加: "\n另外，请将您的纸质营养方案拍照发送给我"│
│    → 患者发图片 → 下载图片 → 多模态LLM识别内容    │
│    → 识别文本写入客户描述                          │
│                                                    │
│  if plan_type == 'electronic':                     │
│    回复: "您的电子营养方案已存档，营养师会为您跟进"│
│    → plan_content 直接写入客户描述                  │
└──────────────────────────────────────────────────┘
┌─ 未找到 ─────────────────────────────────────────┐
│  回复: "未找到您的信息，请确认手机尾号是否正确"  │
└──────────────────────────────────────────────────┘
```

**图片识别流程**：
1. KF `sync_msg` 返回 `msgtype="image"`，含 `media_id`
2. 调用 `media/get` 下载图片到 `uploads/` 目录
3. 调用多模态LLM（GLM-4.5V）识别图片文字内容
4. 识别结果存入 `patient_documents` 表，同时写入客户描述

**相关数据表** `patient_documents`：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| external_userid | TEXT | 患者的KF external_userid |
| phone_tail | TEXT | 手机尾号 |
| doc_type | TEXT | 文档类型：`paper_plan` |
| media_id | TEXT | 企微media_id |
| file_path | TEXT | 本地存储路径 |
| uploaded_at | TEXT | 上传时间 |

---

### 需求4：打标签 + 改备注 + 写描述 ✅ 已完成

**要求**：
- 标签：添加月份（格式 `2026年4月`）、医院全称
- 备注：医院全称+建档姓名（中间用+号，如 "北京协和医院+张三"）
- 客户描述：电子方案直接写 `plan_content`；纸质方案写识别出的图片文字

**企微API**：

1. **创建标签** — `externalcontact/add_corp_tag`
2. **打标签** — `externalcontact/mark_tag`
3. **改备注+描述** — `externalcontact/remark`

**实施函数**：
- `get_or_create_tag(tag_name)` — 先查缓存/远端，不存在则创建
- `tag_customer(employee_userid, external_userid, tag_names)` — 批量打标签
- `update_customer_remark_and_desc(employee_userid, external_userid, remark, description)` — 改备注+描述

---

### 需求5：定时跟进 + 每日报告 ✅ 已完成

- APScheduler 每小时检查：24h后再问尾号，48h仍未回复标记 `no_response` + 备注加"-未回复"
- 每天12:00整理当天未回复患者名单通过自建应用发给员工
- `reception_progress` 扩展字段：`first_ask_time`、`second_ask_time`、`follow_up_count`

---

### 需求6：wmb↔wob ID 映射（核心问题） ✅ 已完成

**背景**：企微的微信客服(KF)和客户联系使用不同的 `external_userid` 体系：

| 系统 | ID 前缀 | 用途 | 示例 |
|------|---------|------|------|
| 微信客服(KF) | `wmb` | kf/send_msg 收发消息 | `wmbYk_FwAAghLUW...` |
| 客户联系 | `wob` | remark/mark_tag 改备注/打标签 | `wobYk_FwAAxtZUN...` |

**企微没有提供 wmb↔wob 的官方转换 API，也没有类似员工 userid 的通用 ID**（unionid 方案需要微信小程序/公众号获取，不适用）。

**实施方案：通过 KF 链接的 `scene` 参数传递 wob ID**

```
1. 员工在自建应用说"给徐子忱发客服链接"
2. 系统从 customers 表查到徐子忱的 wob ID
3. 生成专属链接，scene = "CaoHuiLin:wobYk_FwAAxtZUN..."
4. 员工把链接发给徐子忱
5. 徐子忱点链接进入客服 → enter_session 事件返回 scene 值
6. 系统从 scene 提取 wob ID → 存入 reception_progress.contact_external_userid
7. 后续打标签/改备注直接用存好的 wob ID，无需任何姓名匹配
```

**三层映射策略**：

| 优先级 | 方式 | 可靠性 | 条件 |
|--------|------|--------|------|
| 1 | scene 专属链接 | 100% | 员工指定客户名生成链接 |
| 2 | 姓名精确+模糊匹配 | 看运气 | patients 表的姓名必须和 customers 表一致 |
| 3 | 遍历员工外部联系人 | 较低 | 逐个调API查，有频率限制 |

**数据库改动**：
- `reception_progress` 新增 `contact_external_userid` 字段，存从 scene 获取的 wob ID
- `get_contact_external_userid(kf_external_userid)` — 优先用存储的 wob ID，其次走映射兜底

---

### 需求7：员工查询客服链接 ✅ 已完成

**要求**：员工可通过自建应用获取微信客服链接/二维码

**实施方式**：通过 LLM tool calling，支持自然语言触发：
- "给我客服链接"、"给徐子忱发客服链接"、"怎么让患者联系客服"
- LLM 调用 `get_kf_link(customer_name="徐子忱")` 生成专属链接
- 不指定客户名时生成普通链接（走姓名映射兜底）

**建议**：业务流程上要求员工**必须指定客户名**生成专属链接，确保映射准确。

---

## 三、系统架构（更新后）

```
患者端                 服务器                            企微/DB
  │                     │                                 │
  │──点专属链接────────→│                                 │
  │                     │←──enter_session(scene含wobID)──│
  │                     │──存wobID到DB                    │
  │                     │──kf_send_msg_on_event(欢迎语)──→│──→患者收到欢迎语
  │                     │                                 │
  │──回复"8860"────────→│                                 │
  │                     │←──sync_msg拉取消息──────────────│
  │                     │──查询patients表(8860)           │
  │                     │──用wobID打标签+改备注+写描述────→│
  │                     │──kf_send_msg(群链接+方案提示)──→│──→患者收到回复
  │                     │                                 │
  │──[纸质]发图片──────→│                                 │
  │                     │←──sync_msg(image+media_id)──────│
  │                     │──下载图片→GLM-4.5V识别→更新描述─→│
  │                     │                                 │
  │                     │  [定时任务]                      │
  │                     │──每小时检查未回复→24h再问───────→│──→KF消息给患者
  │                     │──12:00整理名单→发员工消息───────→│──→员工收到名单
  │                     │                                 │
  员工端                │                                 │
  │──"给XX发客服链接"──→│──LLM→get_kf_link(XX)──────────→│──→返回专属链接
  │──"改备注xxx"───────→│──LLM→modify_customer_remark────→│
  │←──结果─────────────│                                 │
```

---

## 四、数据库表汇总

| 表名 | 用途 | 状态 |
|------|------|------|
| customers | 员工-客户关联（含 wob ID） | 已有 |
| reception_progress | 接待进度+跟进状态+contact_external_userid | 已有（已扩展） |
| kf_sync_cursor | KF消息游标 | 已有 |
| kf_chat_history | KF聊天历史 | 已有 |
| kf_conversations | KF会话状态 | 已有 |
| patients | 患者信息（尾号→医院→方案） | 已有 |
| patient_documents | 纸质方案图片存档 | 已有 |
| corp_tags | 企微标签缓存（name→tag_id） | 已有 |

**reception_progress 完整字段**：

| 字段 | 类型 | 说明 |
|------|------|------|
| external_userid | TEXT PK | KF 的 wmb ID |
| employee_userid | TEXT | 员工ID |
| add_time | TEXT | 添加时间 |
| phone_tail | TEXT | 手机尾号 |
| hospital | TEXT | 匹配到的医院 |
| patient_name | TEXT | 匹配到的患者姓名 |
| patient_matched | INTEGER | 是否已匹配患者 |
| welcome_sent | INTEGER | 是否已发欢迎语 |
| phone_received | INTEGER | 是否已收到尾号 |
| status | TEXT | 状态：in_progress/no_response |
| first_ask_time | TEXT | 首次询问时间 |
| second_ask_time | TEXT | 第二次询问时间 |
| follow_up_count | INTEGER | 跟进次数(0/1/2) |
| plan_type | TEXT | 方案类型 |
| group_link | TEXT | 群链接 |
| contact_external_userid | TEXT | **从 scene 获取的 wob ID，用于打标签/改备注** |
| created_at | TEXT | 创建时间 |
| updated_at | TEXT | 更新时间 |

---

## 五、项目文件结构

```
wechat_agent/
├── app/                        # 主应用包
│   ├── __init__.py             # FastAPI app 实例 + load_dotenv
│   ├── config.py               # 配置 + 常量 + 加密器（含 MLLM 配置）
│   ├── database.py             # init_db() + 所有 DB 操作（含 ID 映射）
│   ├── wechat_api.py           # 企微API（token/消息/客户/备注）
│   ├── wechat_kf_api.py        # KF API（sync/send/service_state）
│   ├── patient_service.py      # 患者业务（匹配/标签/备注/图片识别）
│   ├── ai_chat.py              # AI 对话（员工+客户，含 KF 链接 tool）
│   ├── event_handlers.py       # 事件处理（添加好友/外部联系人消息）
│   ├── kf_handler.py           # KF 消息处理（含 scene 提取 wob ID）
│   ├── scheduler.py            # 定时任务（跟进/报告）
│   └── routes.py               # FastAPI 路由
├── tests/                      # 测试脚本
├── uploads/                    # 纸质方案图片存储
├── main_v4.py                  # 入口文件
├── .env                        # 配置文件
└── wecom_cache.db              # SQLite 数据库
```

---

## 六、依赖

```
fastapi          # Web框架
uvicorn          # ASGI服务器
wechatpy         # 企微加解密
requests         # HTTP请求
xmltodict        # XML解析
python-dotenv    # 环境变量
apscheduler      # 定时任务
```

---

## 七、.env 配置项

```
# 企业微信基础配置
WECOM_CORP_ID=
WECOM_SECRET=
WECOM_AGENT_ID=

# 自建应用回调配置
APP_TOKEN=
APP_AES_KEY=

# 客户联系回调配置
CONTACT_TOKEN=
CONTACT_AES_KEY=

# 微信客服(KF)配置
KF_OPEN_KFID=

# 员工配置（定时任务默认发送对象）
DEFAULT_EMPLOYEE_USERID=CaoHuiLin

# AI 配置
OPENAI_API_KEY=
OPENAI_BASE_URL=
MODEL_NAME=

# 多模态AI配置（图片识别）
MLLM_API_KEY=
MLLM_BASE_URL=
MLLM_MODEL_NAME=GLM-4.5V
```

---

## 八、风险与限制

| 项目 | 说明 |
|------|------|
| KF 48小时窗口 | 超过48小时无法再通过KF给患者发消息，24h跟进在窗口内，可行 |
| KF 5条回复限制 | 每条客户消息后最多5条回复，控制在2-3条内，够用 |/
| 图片下载时效 | media_id 有效期3天，需及时下载 |
| 标签同步 | 企微标签创建后缓存tag_id，避免重复创建 |
| 尾号冲突 | 4位数字可能有重复，取最新记录；如有需要可让患者确认姓名 |
| wmb↔wob 无法官方转换 | 企微没有提供转换API，需通过 scene 方案手动关联 |
| 姓名匹配不可靠 | 患者网名可能和真实姓名不一致，必须用专属链接才能保证映射准确 |
| 客户必须先加好友 | 打标签/改备注的前提是该患者是员工的外部联系人 |

---

## 九、待优化项

| 项 | 说明 | 优先级 |
|----|------|--------|
| 尾号确认机制 | 匹配到患者后让AI先确认"您是XXX吗"，防止尾号报错改错人 | 高 |
| 专属链接强制 | 业务流程上强制要求员工指定客户名生成专属链接，禁用普通链接 | 中 |
| 加好友自动关联 | 在 add_external_contact 回调时自动同步 customers 表并尝试关联 | 中 |
| 图片识别容错 | 多模态 LLM 识别失败时标记"需人工查看"并通知营养师 | 低 |
